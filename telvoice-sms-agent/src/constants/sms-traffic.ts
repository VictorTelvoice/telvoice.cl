/** Tope absoluto por cuenta cliente — ninguna configuración puede superarlo. */
export const MAX_CLIENT_TPS = 20;

/** TPS por defecto cuando falta configuración en BD. */
export const DEFAULT_TPS = 1;

/** Ventana del limitador en memoria (segundos). */
export const TPS_WINDOW_MS = 1000;
